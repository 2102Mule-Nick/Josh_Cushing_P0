package proj0.interfaces;

import java.util.Scanner;

import proj0.pojo.UserAcct;

public interface Menu {
	/*
	// Set for a Scanner object
	public void setScan(Scanner scanner);
	
	// Get for a Scanner object
	public Scanner getScan();
	
	
	// Displays items that do not require a logged in user to see.
	public void displayMenuItems(Scanner s);

	// Displays items that do not require a logged in user to see, taking in their
	// account to pass to the next object.
	public void displayWithAcct(UserAcct userAcct, Scanner s);
	 */
}

